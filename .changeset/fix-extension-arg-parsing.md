---
'termcast': patch
---

Fix extension command argument parsing when running against newer `goke` versions.

Compiled extensions and `termcast dev` now start correctly instead of crashing before the command list renders.
